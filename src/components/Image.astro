---
const { src } = Astro.props;
const { alt } = Astro.props;
const { className } = Astro.props;
---

<img
  src={src}
  alt={alt}
  class={className}
  height="auto"
  width="100%"
  loading="lazy"
/>
